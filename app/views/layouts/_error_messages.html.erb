<% if model.errors.any? %>
    <div class = "alert alert-warning">
      <p><%= model.errors.full_messages.length %> errors prohibited this book from being saved:</p>
        <ul>
            <% model.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
        </ul>
    </div>
<% end %>
